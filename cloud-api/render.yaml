# QuoTrading Cloud API - Render Deployment Configuration

# Build Command (Render will run this)
# pip install -r requirements.txt

# Start Command (Render will run this to start your API)
# uvicorn main:app --host 0.0.0.0 --port $PORT

# Environment Variables (set these in Render dashboard):
# - DATABASE_URL (auto-set by Render PostgreSQL)
# - STRIPE_SECRET_KEY (from Stripe dashboard)
# - STRIPE_WEBHOOK_SECRET (from Stripe webhook settings)
# - API_SECRET_KEY (generate random secret)

# Health Check Path: /
# Port: 10000 (default, or use $PORT environment variable)
